[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    gpu: requires a CUDA device
    keras: requires tensorflow/keras (heavy, optional; auto-skipped if absent)
    slow: long-running test
    integration: end-to-end flow on tiny fixtures (CPU)
filterwarnings =
    ignore:A NumPy version:UserWarning
